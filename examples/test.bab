(@ Y (lambda (lambda (if (@ is_nil? $0) empty (cons (@ (@ + (@ car $0)) (@ car $0)) (@ $1 (@ cdr $0)))))))
(@ Y (lambda (lambda (if (@ is_nil? $0) empty (cons (@ (@ - (@ car $0))  1)         (@ $1 (@ cdr $0)))))))
(@ Y (lambda (lambda (if (@ is_nil? $0) empty (cons (@ (@ + (@ car $0))  1)         (@ $1 (@ cdr $0)))))))
(@ Y (lambda (lambda (if (@ is_nil? $0) empty (cons (@ (@ * (@ car $0)) (@ car $0)) (@ $1 (@ cdr $0)))))))

