(list
  ;(lambda (@ (@ + (@ f $0)) (@ f $0)))
  ;(lambda (@ (@ + (@ f $0)) (@ f $0)))
  ;(lambda (@ (@ + (@ f $0)) 1))
  ;(lambda (@ (@ + (@ f $0)) 1))
  (@ f (@ f (@ f (@ f 0))))
  (@ f (@ f (@ f (@ f 0))))
  (@ f (@ f (@ f (@ f 0))))
  (@ f (@ f (@ f (@ f 0))))
  (@ (@ + (@ f (@ f (@ f (@ f 0))))) 
          (@ f (@ f (@ f (@ f 0)))))
  (@ (@ + (@ f (@ f (@ f (@ f 0))))) 
          1)
)