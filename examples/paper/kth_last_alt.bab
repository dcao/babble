;; The shortest way to write k-th last for k = 1, 2, 3;
;; the first program should be rewritten to match the shape of the other two

(lambda (@ (@ index (@ length (@ cdr $0))) $0))
(lambda (@ (@ index (@ (@ - (@ length $0)) 2)) $0))
(lambda (@ (@ index (@ (@ - (@ length $0)) 3)) $0)) 